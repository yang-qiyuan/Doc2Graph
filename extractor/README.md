# Extractor

This service owns the semantic pipeline:
- entity extraction
- relation extraction
- normalization and deduplication
- confidence scoring
- schema-compliant export generation

The implementation starts with fixture-driven development so the contract is stable before model integration.
